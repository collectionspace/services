The schema file here should be generated from the one defined for JAXB in:
{root, e.g., trunk}\services\vocabulary\jaxb\src\main\resources\vocabulary-common.xsd

Soon, we should do this with a tool (XSLT).